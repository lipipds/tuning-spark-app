```shell
pip install -r requirements.txt
pyspark --version
```

```shell
spark-submit --help
http://localhost:4040/jobs/
```

```shell
docker-compose up -d
docker ps

```

```shell
docker-compose down
```